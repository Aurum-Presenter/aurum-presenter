{
	frankenphp
	auto_https off
}

{$SERVER_NAME} {
	root * /app/public
	encode zstd gzip

	# Everything is an API route; there are no static assets served from here. The PWA is
	# built separately and hosted as static files.
	php_server
}
